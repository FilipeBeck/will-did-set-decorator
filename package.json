{
  "name": "will-did-set-decorator",
  "version": "0.0.4",
  "description": "Class property decorator to handle will-set and did-set",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/FilipeBeck/will-did-set-decorator"
  },
  "author": "Filipe Roberto Beck <filipe.beck@gmail.com>",
  "license": "MIT",
  "private": false,
  "scripts": {
    "prebuild": "rm -rf ./app",
    "build": "tsc --tsBuildInfoFile /dev/null",
    "build:package": "pnpm build && cp *.md package.json ./app",
		"test": "jest",
		"test:cov": "jest --coverage"
  },
  "peerDependencies": {
    "tslib": "^2.3.1"
  },
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "jest": "^27.2.5",
    "ts-jest": "^27.0.5",
    "tslib": "^2.3.1",
    "typescript": "^4.4.3"
  }
}
